# Supabase — placeholders for the storage swap that is not wired up yet.
# Today the app always uses the local JSON store in .data/; see the "Storage"
# section of the README for what changes when these are filled in.
NEXT_PUBLIC_SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
